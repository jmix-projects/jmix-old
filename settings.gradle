rootProject.name = 'jmix'

include 'jmix-core'
include 'jmix-backend'
include 'starters:starter-standard'
include 'samples:sample-hello-world'

rootProject.children.each { p1 ->
	p1.buildFileName = "${p1.name}.gradle"
	p1.children.each { p2 ->
		p2.buildFileName = "${p2.name}.gradle"
	}
}